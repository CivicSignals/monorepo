# docs/

Source for the public docs site (docs.civicsignals.io — Docusaurus or similar,
TODO Q1). Product docs (Q2), API docs generated from OpenAPI (Q3), self-host
docs (Q4), and the recipe authoring guide (Q5) live here.

> The pre-build planning package (product brief, PRD, architecture, data model,
> API spec, delivery plan, etc.) is kept locally as reference and is gitignored
> at the repo root — it is not part of the published docs site.

The self-host configuration reference (every env var; TODO O4) will live at
`docs/self-host/config.md`.
