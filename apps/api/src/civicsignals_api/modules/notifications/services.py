"""Public service interface for the notifications module.

Other modules call notifications only through the functions defined here — never by
importing notifications's models or routes directly (doc 06 §3).
"""
from __future__ import annotations
