"""Public service interface for the extraction module.

Other modules call extraction only through the functions defined here — never by
importing extraction's models or routes directly (doc 06 §3).
"""
from __future__ import annotations
