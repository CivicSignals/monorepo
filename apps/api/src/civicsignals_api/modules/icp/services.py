"""Public service interface for the icp module.

Other modules call icp only through the functions defined here — never by
importing icp's models or routes directly (doc 06 §3).
"""
from __future__ import annotations
