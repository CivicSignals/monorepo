{
  "name": "@civicsignals/api",
  "version": "0.1.0",
  "private": true,
  "description": "Turborepo task shim for the Python API. Real work runs under uv.",
  "scripts": {
    "dev": "uv run uvicorn civicsignals_api.main:app --reload --port 8000",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "format": "uv run ruff format . && uv run ruff check --fix .",
    "typecheck": "uv run mypy src",
    "test": "uv run pytest"
  }
}
