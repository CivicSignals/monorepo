---
description: >
  Smart Search NL -> structured query rewrite (doc 06 §2, doc 16 Smart Search).
  Turns a free-text user query into the structured filter dimensions the signal
  feed understands. Cheap Haiku-class; latency-sensitive (typed in the UI).
task: classify
model_hint: anthropic:claude-3-5-haiku-latest
variables: [query]
system: >
  You convert natural-language searches over US public-sector buying signals into
  a structured query. You answer only with the requested JSON and nothing else.
---
Rewrite the user's search into a structured query over the signal feed.

Recognised dimensions:
- `signal_type`: one or more of rfp_posted, rfi_rfq, contract_expiring,
  contract_awarded, budget_approved, grant_awarded, grant_opportunity,
  leadership_change, board_agenda_item, strategic_plan_published, open_job,
  news_mention
- `state`: two-letter US state codes (e.g. TX, CA)
- `entity_kind`: e.g. school_district, city, county, state_agency, university
- `keywords`: free-text terms for full-text + semantic ranking
- `amount_min_cents` / `amount_max_cents`: deal-band bounds when implied

Output JSON exactly matching this shape, omitting dimensions the query does not
constrain:
{{"signal_type": [str], "state": [str], "entity_kind": [str], "keywords": [str],
  "amount_min_cents": int|null, "amount_max_cents": int|null}}

User query:
<<<
{query}
>>>
