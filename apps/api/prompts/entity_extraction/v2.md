---
description: >
  Stage 3 Pass-1 entity extraction (doc 19 §4). The cheap first pass: extract
  candidate named entities (organizations, persons, monetary amounts, dates,
  products, vendors, contract terms) directly from the document text. No LLM
  validation or cross-reference — just surface what is there. Pass 2 (v1) then
  refines, validates, and fills gaps.
task: extraction
model_hint: anthropic:claude-3-5-haiku-latest
variables: [document_text]
system: >
  You extract raw structured facts from public-sector documents. Your job is
  a fast first pass — surface every organization, person, amount, date, product
  category, vendor name, and contract term you can see. Include anything that
  looks like it could be relevant; Pass 2 will validate. Answer only with the
  requested JSON and nothing else.
---
Extract all named entities from the document below. Be inclusive — prefer
false positives over missed entities.

Output JSON matching this schema exactly (each list item carries its own
`confidence` 0.0–1.0; use `source_offset` when you know the character position):

{{
  "organizations": [{{"name": str, "confidence": float, "source_offset": int|null}}],
  "persons": [{{"name": str, "role": str|null, "confidence": float}}],
  "monetary_amounts": [{{"amount_cents": int, "currency": str, "confidence": float}}],
  "dates": [{{"value": str, "kind": str, "confidence": float}}],
  "products_categories": [str],
  "vendors_mentioned": [{{"name": str, "confidence": float}}],
  "contract_terms_mentions": [{{"text": str, "confidence": float}}],
  "raw_keywords": [str],
  "extraction_confidence": float,
  "extraction_warnings": [str]
}}

Rules:
- `extraction_confidence` is your overall confidence for this pass (0.0–0.95).
- Cap all per-entity `confidence` values at 0.95.
- `dates.value` must be ISO-8601 (`YYYY-MM-DD` or `YYYY-MM-DDTHH:MM:SSZ`).
- `monetary_amounts.amount_cents` is integer cents (e.g. $400,000 → 40000000).
- If nothing is found, return empty lists and `extraction_confidence: 0.0`.

Document:
<<<
{document_text}
>>>
