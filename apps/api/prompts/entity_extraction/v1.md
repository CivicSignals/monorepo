---
description: >
  Stage 3 LLM-assisted entity extraction (doc 19 §4). The Sonnet-class second
  pass, run only when the deterministic first pass found too few entity types or
  was low-confidence. Validates the partial extraction and fills missing fields.
task: extraction
model_hint: anthropic:claude-3-5-sonnet-latest
variables: [document_text, partial_entities]
system: >
  You extract structured facts from public-sector documents — organizations,
  persons, monetary amounts, dates, product categories, vendors, and contract
  terms. You answer only with the requested JSON and nothing else.
---
A deterministic first pass already extracted some entities from the document
below. Your job is to (a) validate that partial extraction, (b) fill in fields
it missed, and (c) note anything unusual.

Output JSON matching this schema (each list item carries its own
`confidence` 0.0-1.0 and, where known, a `source_offset` character index):

{{
  "organizations": [{{"name": str, "confidence": float, "source_offset": int|null}}],
  "persons": [{{"name": str, "role": str|null, "confidence": float}}],
  "monetary_amounts": [{{"amount_cents": int, "currency": str, "confidence": float}}],
  "dates": [{{"value": str, "kind": str, "confidence": float}}],
  "products_categories": [str],
  "vendors_mentioned": [{{"name": str, "confidence": float}}],
  "contract_terms_mentions": [{{"text": str, "confidence": float}}],
  "raw_keywords": [str],
  "extraction_confidence": float,
  "extraction_warnings": [str]
}}

Cap `extraction_confidence` at 0.95 — perfect certainty is suspicious.

Partial extraction from the first pass:
{partial_entities}

Document:
<<<
{document_text}
>>>
