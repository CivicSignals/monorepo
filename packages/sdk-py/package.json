{
  "name": "@civicsignals/sdk-py",
  "version": "0.1.0",
  "private": true,
  "description": "Turborepo task shim for the Python SDK. Real work runs under uv.",
  "scripts": {
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "typecheck": "uv run mypy src",
    "test": "uv run pytest"
  }
}
