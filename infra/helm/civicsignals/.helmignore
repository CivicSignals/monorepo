# Patterns to ignore when building packages.
# Follows the same rules as .gitignore.
.DS_Store
# CI artifacts
*.orig
# NOTE: do NOT ignore *.lock — Chart.lock must be included for reproducible builds.
